<Page x:Class="Organizer.StatsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:DVC="clr-namespace:System.Windows.Controls.DataVisualization.Charting;assembly=DotNetProjects.DataVisualization.Toolkit"
      xmlns:local="clr-namespace:Organizer" 
      mc:Ignorable="d" 
      FocusManager.IsFocusScope="True"
      Title="StatsPage" Height="560" Width="1080" MinWidth="1080" MinHeight="560">

    <Grid Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" VerticalAlignment="Stretch">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.Resources>
            <local:PlotCollection x:Key="PlotCollection" />
        </Grid.Resources>

        <!--<DVC:Chart Canvas.Top="80" Canvas.Left="10" Name="mcChart" Margin="574,25,10,25" BorderBrush="{x:Null}">
            <DVC:Chart.Series>
                <DVC:BarSeries Title="Runtime"  
                    ItemsSource="{StaticResource PlotCollection}"  
                    IndependentValueBinding="{Binding Path=Name}"  
                    DependentValueBinding="{Binding Path=Time}">
                    <DVC:BarSeries.LegendItemStyle >
                        <Style TargetType="DVC:LegendItem">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </Style>
                    </DVC:BarSeries.LegendItemStyle>
                    <DVC:BarSeries.DependentRangeAxis>
                        <DVC:LogarithmicAxis Minimum="0" Maximum="200"/>
                    </DVC:BarSeries.DependentRangeAxis>
                </DVC:BarSeries>
            </DVC:Chart.Series>
        </DVC:Chart>-->


        <DVC:Chart Title="Sample Chart" Margin="585,69,46,54" BorderBrush="{x:Null}">
            <DVC:Chart.Axes>
                <DVC:LinearAxis Minimum="0" Orientation="Y" ShowGridLines="True" />
            </DVC:Chart.Axes>
            <DVC:ColumnSeries ItemsSource="{StaticResource PlotCollection}"
                              IndependentValueBinding="{Binding Path=Name}"  
                              DependentValueBinding="{Binding Path=Time}" />

            <DVC:Chart.LegendStyle>
                <Style TargetType="Control">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Setter Property="Width" Value="0"/>
                    <Setter Property="Height" Value="0"/>
                </Style>
            </DVC:Chart.LegendStyle>
        </DVC:Chart>


        <ListBox x:Name="profilesList" d:ItemsSource="{d:SampleData ItemCount=5}" Margin="40,69,884,225" Height="156" SelectionChanged="profileSelectionChanged">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock>
                        <TextBlock.Text>
                            <MultiBinding StringFormat="{}{0} - {1}">
                                <Binding Path="Id_prof"/>
                                <Binding Path="Name"/>
                            </MultiBinding>
                        </TextBlock.Text>
                    </TextBlock>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <ListBox x:Name="programsList" d:ItemsSource="{d:SampleData ItemCount=5}" Margin="40,240,884,54" Height="156" SelectionChanged="programSelectionChange">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock>
                        <TextBlock.Text>
                            <MultiBinding StringFormat="{}{0} - {1}">
                                <Binding Path="Id_prog"/>
                                <Binding Path="Name"/>
                            </MultiBinding>
                        </TextBlock.Text>
                    </TextBlock>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <ListBox x:Name="statsList" d:ItemsSource="{d:SampleData ItemCount=5}" Margin="290,69,533,54">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Id_prof}"/>
                        <TextBlock Text="{Binding Id_prog}"/>
                        <TextBlock Text=" "/>
                        <TextBlock Text="{Binding Time_start}"/>
                        <TextBlock Text=" - "/>
                        <TextBlock Text="{Binding Time_stop}"
                       HorizontalAlignment="Right"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

    </Grid>
</Page>
